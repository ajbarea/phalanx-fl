# Docker Compose Configuration
# Usage:
#   docker compose up              # Development (with monitoring, hot reload)
#   docker compose -f docker-compose.yml up -d  # Production only (no monitoring, no overrides)
#   docker compose run --rm api python -m intellifl.simulation_runner <config.json>

services:
  api:
    image: intellifl-api:latest
    build: .
    pull_policy: always
    container_name: intellifl-api
    restart: unless-stopped
    ports:
      - "${API_PORT:-8000}:8000"

    # Shared memory for PyTorch DataLoader workers
    shm_size: '4gb'

    volumes:
      - ./out:/app/out                    # Simulation outputs (persistent)
      - ./datasets:/app/datasets          # Datasets (auto-downloaded on first run)
      - ./config:/app/config:ro           # Configs (read-only)

    env_file: .env
    environment:
      PYTHONUNBUFFERED: 1

    # GPU support (ignored on CPU-only machines)
    deploy:
      resources:
        reservations:
          memory: 8G
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s  # First run downloads datasets

  frontend:
    image: intellifl-web:latest
    build: ./frontend
    pull_policy: always
    container_name: intellifl-web
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-80}:80"
    depends_on:
      api:
        condition: service_healthy
    env_file: .env
    deploy:
      resources:
        limits:
          memory: 512M

  # ============================================================================
  # Documentation (Zensical)
  # ============================================================================

  docs:
    image: python:3.11-slim
    container_name: intellifl-docs
    restart: unless-stopped
    working_dir: /app
    command: >
      sh -c "pip install --quiet zensical && zensical serve --dev-addr 0.0.0.0:8000"
    ports:
      - "${DOCS_PORT:-8080}:8000"
    volumes:
      - ./docs:/app/docs:ro
      - ./zensical.toml:/app/zensical.toml:ro
    deploy:
      resources:
        limits:
          memory: 256M
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # ============================================================================
  # Celery Task Queue Infrastructure
  # ============================================================================

  redis:
    image: redis:7-alpine
    container_name: intellifl-redis
    restart: unless-stopped
    command: redis-server --maxmemory 512mb --maxmemory-policy allkeys-lru
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  celery-worker:
    image: intellifl-api:latest
    container_name: intellifl-celery-worker
    restart: unless-stopped
    command: celery -A intellifl.celery_app worker --loglevel=info --concurrency=${CELERY_CONCURRENCY:-1}
    shm_size: '8gb'
    volumes:
      - ./out:/app/out
      - ./datasets:/app/datasets
    env_file: .env
    environment:
      PYTHONUNBUFFERED: 1
    depends_on:
      redis:
        condition: service_healthy
    deploy:
      resources:
        reservations:
          memory: 8G
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD-SHELL", "celery -A intellifl.celery_app inspect ping -d celery@$HOSTNAME || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

volumes:
  redis-data:

networks:
  default:
    driver: bridge
    driver_opts:
      com.docker.network.bridge.enable_icc: "true"
    ipam:
      config:
        - subnet: 172.20.0.0/16
